# shared/services/

Serviços transversais de backend reutilizados por vários módulos
(ex.: notificações, geração de relatórios). Regras específicas de um único
módulo permanecem dentro do próprio módulo em `modules/`.
